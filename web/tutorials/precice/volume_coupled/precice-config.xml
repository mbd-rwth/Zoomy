<?xml version="1.0" encoding="UTF-8"?>
<precice-configuration>
  <log>
    <sink
      filter="%Severity% > debug and %Rank% = 0"
      format="---[precice] %ColorizedSeverity% %Message%"
      enabled="true" />
  </log>

  <profiling mode="fundamental" synchronize="false" />

  <!-- Define two data names for two-way coupling -->
  <data:vector name="Velocity" />
  <data:vector name="VelocityBack" />

  <!-- Mesh declarations -->
  <mesh name="Fluid1-Mesh" dimensions="3">
    <use-data name="Velocity" />
    <use-data name="VelocityBack" />
  </mesh>

  <mesh name="Fluid2-Mesh" dimensions="3">
    <use-data name="Velocity" />
    <use-data name="VelocityBack" />
  </mesh>

  <!-- Participant Fluid1 -->
  <participant name="Fluid1">
    <provide-mesh name="Fluid1-Mesh" />
    <receive-mesh name="Fluid2-Mesh" from="Fluid2" />

    <!-- Fluid1 sends Velocity, receives VelocityBack -->
    <write-data name="Velocity"     mesh="Fluid1-Mesh" />
    <read-data  name="VelocityBack" mesh="Fluid1-Mesh" />

    <mapping:nearest-neighbor
      direction="read"
      from="Fluid2-Mesh"
      to="Fluid1-Mesh"
      constraint="consistent" />
  </participant>

  <!-- Participant Fluid2 -->
  <participant name="Fluid2">
    <provide-mesh name="Fluid2-Mesh" />
    <receive-mesh name="Fluid1-Mesh" from="Fluid1" />

    <!-- Fluid2 sends VelocityBack, receives Velocity -->
    <write-data name="VelocityBack" mesh="Fluid2-Mesh" />
    <read-data  name="Velocity"     mesh="Fluid2-Mesh" />

    <mapping:nearest-neighbor
      direction="read"
      from="Fluid1-Mesh"
      to="Fluid2-Mesh"
      constraint="consistent" />
  </participant>

  <!-- Communication channel -->
  <m2n:sockets acceptor="Fluid2" connector="Fluid1"
               exchange-directory="/home/ingo/precice-coupling" />

  <!-- Coupling scheme -->
  <coupling-scheme:parallel-implicit>
    <time-window-size value="0.001" />
    <max-time value="0.5" />
    <participants first="Fluid2" second="Fluid1" />

    <!-- Data exchange definitions -->
    <exchange data="Velocity"     mesh="Fluid1-Mesh" from="Fluid1" to="Fluid2" initialize="yes" />
    <exchange data="VelocityBack" mesh="Fluid2-Mesh" from="Fluid2" to="Fluid1" initialize="yes" />

    <max-iterations value="3" />
    <relative-convergence-measure limit="1.0e-5" data="Velocity"     mesh="Fluid1-Mesh" />
    <relative-convergence-measure limit="1.0e-5" data="VelocityBack" mesh="Fluid2-Mesh" />
  </coupling-scheme:parallel-implicit>
</precice-configuration>

